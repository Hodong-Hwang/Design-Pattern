#include <iostream>
#include <list>
#include <stack>
#include <set>

struct Point
{
    int x;
    int y;
};

int main()
{
    std::list<Point> s;
    Point pt ;
    s.push_back(pt);

    std::list<Point>::value_type n;

}